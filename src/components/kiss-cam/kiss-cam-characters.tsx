"use client";

/**
 * Kiss Cam characters — layered PNG puppets (see kiss-cam-puppet.tsx).
 * Legacy SVG illustration remains in this file as `_LegacySvg*` for reference only.
 * Pose animation still comes from kiss-cam-pose.ts.
 */

export { GroomFigure, BrideFigure } from "@/components/kiss-cam/kiss-cam-puppet";
